# 更新日志 / Changelog

## 未发布（v0.3.8 开发中）

### Modbus 标准协议
- 解析引擎表达力升级：帧头与识别位支持**逐字节位掩码**（`??` 通配 / `A?` 半字节 / `80&F0` 显式掩码）、长度域支持**倍率**（位数→字节）、32 位量支持 **CDAB / BADC 交换字序**
- **Modbus RTU 预设扩为 13 种帧型**、新增 **Modbus TCP 预设（15 种帧型）**：一条模板吃下整条总线的任意从站地址；读响应把寄存器区展开为 `寄存器1..N`、线圈区按位展开为 `线圈1..N`（一位一通道）；异常响应用 `FC & 0x80` 掩码一条覆盖所有功能码
- Modbus TCP 没有 CRC：改用 MBAP 长度域的**奇偶**区分主站请求与从站响应，嗅探 502 端口时两侧不再互相误判
- 演示数据流混入一路 Modbus 主从轮询（含异常响应与坏 CRC），无硬件也能体验定帧、整流与错位自愈
- 新增**值标签（枚举注解）**：给状态码 / 异常码配文字，表格、悬停与 CSV/Excel 导出显示「2 非法数据地址」，数值通道与曲线仍用原始数字
- 解析健壮性：校验失败时按帧头逐层重新同步（修复重同步吞帧），主站轮询与从站应答混排时按有效行覆盖关系剔除噪声坏帧
- AI 助手知识同步：识别位/掩码/倍率/字序/数组区/值标签全部写入协议规范，判定为 WIT·匿名V7·Modbus·NMEA·JustFloat 时直接引导使用内置预设

### Modbus 主从侧（新增「Modbus 工作台」面板）
- 新增面板 **Modbus 工作台**：一屏两页，把「没有硬件也想测 Modbus」这件事补齐
  - **模拟从站**：本机当从站应答总线。四张可编辑数据表（线圈 / 离散输入按位点选，保持 / 输入寄存器直接填数并可切 HEX、悬停显示 40001 式手册编号），可设从站地址或「应答所有地址」当多从站；**故障注入**支持不回应答（复现主站超时）、一律回指定异常码、隔一次回一次异常，并可加应答延时模拟慢速从站；主站写入的值实时反映在表内，广播语义（读不应答、写执行不答）按规范实现
  - **主站轮询表**：每行「从站 + 功能码 + 起始地址 + 数量 + 周期 + 变量名 + 元素序号 + 倍率」，按周期发读请求，**响应在本模块解析后直接写成变量**——不用再配协议模板，曲线 / 表格 / 脚本按名引用即可
  - 半双工安全：同一时刻只允许一条在途请求，1 秒无应答记一次超时并放行下一条；从站与轮询互斥启动（自己问自己答会得出"设备健康"的假象）
  - 两项服务都在面板之外运行：**关掉面板仍在应答/轮询**，工具栏出现绿色徽标提示并一键回到工作台
- 指令工厂补齐发送侧：Modbus RTU 新增 **FC15 / FC16 写多点**（线圈按位打包、寄存器逐字大端），起始地址支持直接抄手册编号自动换算；新增 **Modbus TCP 编解码器**（MBAP 头自动组、事务号可自增、无 CRC）
- 帧格式与接口解耦：RTU / TCP 由用户显式选择——RTU 常跑在 TCP 串口隧道上，反之不成立，故 Modbus TCP 仅在网络接口可用
- AI 可直接操作工作台：新增 `modbus` 动作（`slave.start/stop/configure/write/writeMany/resize`、`poll.add/remove/clear/configure/start/stop/reset`、`status`），"把 40003 设成 1234""每 500ms 读 1 号从站 10 个寄存器""让它别应答试试"都能一句话完成
- 发送侧统一走新增的 Modbus 内核模块（编解码 / 数据区模型 / 按方向定长的流式切帧），指令工厂、模拟从站、主站轮询共用一套实现；前端测试由 84 项增至 **149 项**（新增内核 25、指令工厂 7、模拟从站 12、主站轮询 13）

## v0.3.7 (2026-09-09)

自 v0.3.6 之后的累计变更（P35 ~ P56c）。

### 会话录制回放
- 录制完整调试会话（解析帧流 + 原始收发字节），保存为 `.usess` 文件随时回放
- 回放引擎与演示源同构：帧画布 / 2D 曲线 / 数据表格 / 3D 姿态 / Hex 数据流全面板同步倒带
- 0.25× ~ 4× 速度档、进度条点选跳转、时间轴标注（回放中按 M 打点，随文件保存，2D 曲线画琥珀标记线）
- 回放桥接：TCP 服务端把录制会话变成「虚拟设备」，第三方上位机连入即收到与真机同节奏的字节流

### 数据接口与传输
- 新增蓝牙 BLE 接口：扫描 / 连接 Nordic UART 透传模块（nRF52 / ESP32 等），收发与串口完全同源
- 新增 XMODEM / XMODEM-1K / YMODEM 文件发送（Bootloader / IAP 烧录），传输期间自动截流设备应答，不污染解析与录制

### 协议解析与帧画布
- 「结构发现」面板：未知协议自动测帧长 / 相位 / 列熵，帧头候选一键生成协议模板
- spanTail 自适应载荷字段：延伸至载荷尾，可按元素序列展开成 `字段#1..N` 变量
- 负偏移（尾锚）字段：距帧尾定位，随帧长自适应；校验域在变长模式下锚定帧尾
- 校验闭环：画校验字段即启用算法，字段宽度与算法一致性校验，人话报错
- 图例组眼睛与子变量管理（单眼睛 / 右键删除 / 随最新帧伸缩）
- 数据表格三级级联列菜单（协议簇 → 帧型 → 字段，三态勾选）

### 2D 曲线
- 快照叠加：冻结当前视野为参考虚线，拖到别处叠画对比
- 右键菜单分层重构：视图动作 / 设置（坐标轴 / 曲线样式 / 游标与网格）/ AI

### AI 助手
- 协议簇批量写入：多帧型协议一次生成整簇（`{group, templates[]}`），自动建组归档
- 图片多模态：附件按钮 + Ctrl+V 粘贴截图（每条最多 4 张，自动压缩），AI 直接看图回答
- 新增 `xferStart` 动作：AI 可预填固件烧录对话框（开始发送仍需人工确认），白名单动作增至 28 个

### 界面
- 设置 / 帮助弹窗定尺寸（896×640，小窗按比例收缩，任意缩放档位不变形）
- 扩展管理多选打包导出；控制画布「⋯」溢出菜单

### 结构发现增强（协议簇）
- 协议簇发现：对候选帧头做「帧头家族 + 帧间距直方图」分析，混发多帧型协议（如 WIT 0x55 系）不再被单周期算法摊薄——识别出每种帧型的帧长
- 恒定段每行「簇分析」按钮：帧头/长度字段统计打平时（相位二义），把分析种子选择权交给用户
- 簇卡片勾选帧型 → 一键按勾选批量建模板；模板面板多帧型组新增「＋添加帧型」
- 分析参数行：显著度阈值 / 帧长上限 / 分析窗口可调；采样缓冲扩至 256KB 冻结快照（统计与渲染不再错位）

### 性能与构建
- release 编译调优（LTO + 单 CGU + strip）：安装包体积与热路径性能改善
- Excel 导出迁移到 Rust 侧（移除含 CVE 的 npm xlsx 依赖）
- debug 启动白屏自愈：dev server 未就绪时主窗口直载引导页（HTTP 探活 + ↻ 重试 / × 关闭）

### UI 体系
- 设计令牌层（间距 / 字号 / 圆角 / 动效时长）+ 系统级减少动效自动降级
- 数值位表全面 tabular-nums（实时读数不再左右抖动）；对话框 / 菜单无障碍语义补全
- 新增 Okabe-Ito 色觉友好曲线色板（设置可切换）；主题对比度 CI 校验并修复三主题按钮文字可读性（新增 `--on-accent`）
- theme.css 按主题拆分（5518 行 → 入口 + 8 个主题文件）；帧画布 / 2D 曲线纯逻辑抽离 + 35 项单测锁死历史 bug 规则

### AI 助手（P56c 知识同步 + 预设刷新）
- AI 服务预设全面更新为 2026-09 官方现行模型 ID：`gpt-5.6-sol` / `deepseek-v4-pro` / `glm-5.3`（新增智谱 GLM 预设）/ `qwen3.8-flash` / `claude-sonnet-5` / Ollama `gemma4:12b`；旧配置中已停服的模型名（如 `deepseek-chat`）自动迁移
- AI 知识库同步：五类数据接口（串口 / TCP 客户端 / TCP 服务端 / UDP / BLE）、结构发现与协议簇、会话录制回放、文件传输；可生成摇杆 / 键盘遥控 / 单键遥控卡片；主题生成内置按钮对比度硬规则与设计令牌清单

### 质量护栏
- ESLint 全库 0 error + vitest 64 用例 + aria / 主题对比度两个 CI 检查脚本；每轮交付必跑验证套件

### 修复
- **修复启动即卡死**：会话状态查询在持锁期间重复加锁导致主线程自死锁（v0.3.6 后引入，每次启动必现）
- 修复回放命令静默失败（State 未注册）；修复「已连接」后状态栏无接口描述
- 修复面板页签拖入其他分组显示禁止图标（dockview v8 指针拖拽策略，WebView2 兼容）
- 修复控制台开时间戳后换行悬挂缩进（行内容改块级布局，时间戳贴左）；工具条按钮与文字基线对齐
- 修复 BLE 接口在 AI 上下文中被误标为串口

## v0.3.6 (2026-09-04)

AI 助手大版本：能创造、能执行、能感知；帧画布易用性补全；深度面板中英双语。

### AI 助手
- 十种创作块一键安装：协议模板 / 控制卡片（含 custom 沙箱卡片）/ 命令库 / 指令工厂协议 / 主题 / 全局样式 / 停靠面板 / 沙箱小部件 / 无边框挂件 / uartix-action 直接执行 / 高权限脚本
- 「说出来即发生」：27 个白名单 App 动作，操作类诉求由 AI 直接执行；破坏性动作红标 + 人工确认
- `window.uartix` 注入桥（所有沙箱组件自动可用）：键盘 / 光标监听、onChat 思维链感知、ask 提问（忙时排队上限 3）、menu.define 多命名菜单 + 子菜单、win.* 窗口控制、broadcast 挂件互感、speak 语音播报、onTheme 主题订阅
- 无边框形态：透明窗 + 按住即拖（dragDelta 增量模型，根治 Windows 粘滞拖拽）+ 松开停靠 + 边界钳制 + 60 秒点击穿透自愈
- 思维链多段 rounds 展示（续写轮另起新框）；安装卡流式守卫（closed 前不给按钮）
- token 经济：轻底座 + `[[need]]` 自取续写 + 关键词路由预注入 + 两级上下文摘要 + token 估算
- 主题桥：collectThemeVars 采集 14 个 CSS 变量 + data-theme，经 init/theme-req/broadcast 注入沙箱组件，换肤实时跟随
- 分享包一键导入卡（uartix-extensions JSON 块解析统计 + importAll）

### 帧画布与协议
- 协议簇 / 单协议右键导出 JSON 分享包（uartix-templates v1，导入以副本追加）
- 骨架态「未见有效帧」徽标（含四步排查提示，区分"从未匹配/回看历史"）
- 导入预设防呆（重复 presetKey 计数确认）
- 帧头 / 帧尾解析统一收口 shared/hexBytes：支持清空、非法红框（无帧头逗号文本流合法）
- 新建协议对话框：单协议 / 协议簇 / 自适应文本帧三合一

### 界面与体验
- 深度面板中英双语（方案 C：UI chrome 全迁，帮助正文留中文）：帧画布 / 属性 / 控制画布 / 卡片设置 / 协议模板 / 面板页签即时切换；机制 `tx(zh,en)` + `useLocale` + dockview `setTitle` 重挂
- 默认主题改为海棠（全新安装生效）；面板稳定选择器 `[data-panel]`（AI 样式作用域不再依赖 dockview 内部类名）
- 自定义布局槽位（另存 / 切换 / 删除，上限 12，切预设前 auto 备份）
- 帮助新增「AI 助手详解」页；控制画布流式排布修复（findFlowPos 行优先）

## v0.3.0 (2026-08-31)

性能与可视化体验大版本：2D 曲线全面重构、IPC 二进制化、面板生命周期门控、拔线自愈。

### 2D 曲线
- 双游标测量：时间尺（垂直）+ 幅度尺（水平）独立控制，面板可拖拽、位置持久化
- 指针十字贴近曲线交点显示原始 x/y 读数（按通道原值）
- 时间轴改相对秒（0s / 60s / 1.2h），最新线锚定可见通道真实末点
- Y 轴自动贴合持续生效；新增一次性 Auto 键（X+Y 一步适配最佳视野）
- 峰谷保形抽稀覆盖全部通道（每桶 min/max + 桶首末 + 序列末点），平坦/隐藏通道不再引起曲线崎岖
- 视窗裁剪喂数：重绘成本只与视野内点数相关；悬空 X 源自动纠正回时间源

### 性能与稳定
- IPC 全面二进制化：帧数据经 Tauri Channel 直推 ArrayBuffer，去 base64/JSON，字符串按批字典去重
- 面板生命周期门控：关闭即停（重开从新数据继续），后台标签页只缓冲不渲染，前台全速
- 收流期间 App 本体不再被拖动全量重渲染（计数器/遥测拆分为叶子订阅）
- 串口命令异步化（枚举/打开/发送移出主线程）；GPU 合成恢复，渲染回到显卡
- 拔线检测与自动重连：串口 2 秒无数据核对端口存在性，TCP 连接超时 3 秒，UDP 错误容错续跑

### 功能
- 视频传输（图传）面板：网络图传源、解析设置、暂停/继续/镜像/翻转/保存
- 控制画布：卡片复制粘贴、几何保护（最小 1×1）、统一定量网格对齐
- 键盘遥控卡片锁定 n×n；脚本模式自动填充示例脚本
- 3D 姿态变量支持跨模板选择（模板簇 → 帧型 → 字段层级下拉）
- 新主题：深蓝/浅蓝、护眼绿（matcha）、琥珀（amber）、海棠（浅）、琉璃（深）；色卡网格选择
- 波特率输入支持手动输入 + 14 档预设下拉
- 设置页 About 作者卡片（Tanix，链接官网）

### 工程地址
- 下载地址永久固定：`releases/latest/download/<固定文件名>`，官网/外链一次配置永不变

## v0.2.0 (2026-08-29)

### 网络接口
- TCP 客户端 / TCP 服务端 / UDP 三种网络源，与串口同源数据管线
- 自动更新框架（tauri-updater，GitHub Releases 分发 latest.json）

### 稳定性与交互
- P7~P10 系列：约 45 项修复与增强
- 稳定性大修（事件丢失、指针捕获、内存上限）
- 交互增强与拖拽重写（ghost 预览、2 格碰撞半径、无重叠保证）
- 指令工厂：命令库树形分组拖拽、类 C 脚本模式、自定义协议模板创建

## v0.1.0 (2026-08-27)

首个公开版本。

### 协议解析
- Hex 画布拖拽框选定义帧结构（帧头 / 长度位 / 识别位 / 载荷 / 校验 / 保留尾）
- 多协议模板并行路由，识别位通配；协议簇分组管理
- Sum8 / XOR8 / CRC16-Modbus / CRC16-CCITT / CRC32 校验
- 10 种字段类型、大小端、缩放偏置；CSV 文本流自适应通道
- 内置 WitMotion JY901 预设（10 帧型）与 CSV 演示协议

### 可视化
- 2D 曲线（每通道独立缓冲、框选缩放、X 轴挂载任意变量）
- 3D 姿态（欧拉角 6 旋转序 / 四元数、四轴与立方体模型）
- 数据表格虚拟列表、CSV / Excel 导出；Hex 控制台

### 下行控制
- 控制画布：滑条 / 按钮 / 开关 / 多档开关 / LED / 数值监视 / 摇杆
- 正方形网格吸附（N×M 行列、格尺寸 60/72/90/110 可调）、摇杆锁定正方形
- 类 C 脚本（send / beep / delay_ms）、命令库树形分组、变量系统

### 工程
- 四套工作区预设布局、JSON 导入导出三件套（协议 / 画布 / 命令）
- 自绘标题栏（置顶、主题跟随）、深浅主题、中文界面
- Tauri 2 + React 19 + Rust；Windows x64 NSIS 安装包
